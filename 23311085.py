def main(csvfile, adults, type):
    with open(csvfile) as f:
        content = []
        for line in f:
            print(line)
            content.append(line)
    
def Asymmetry(x,y,z):
    return pow((pow(x,2) + pow(y,2) + pow(z,2)),0.5)

def Standard_deviation_upp(list):
